[project]
name = "voice-transcriber"
version = "0.1.0"
description = "macOS voice transcription app using Parakeet-MLX"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "parakeet-mlx>=0.1.0",
    "mlx-lm>=0.19.0",
]

[tool.uv]
dev-dependencies = []
